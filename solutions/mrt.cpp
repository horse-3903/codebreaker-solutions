#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main(){
    ll n, e, q;
    cin >> n >> e >> q;

    ll x, y, t;
    ll a, b;
    vector<ll> p[n];

    for (ll i = 0; i < e; i++){
        cin >> x >> y >> t;
        p[n]
    }

    for (ll i = 0; i < q; i++){
        cin >> a >> b;
        
    }
}