#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main(){
    ll n, l, t;
    cin >> n >> l >> t;

    ll d = 0;

    ll f[n];
    for (ll i = 0; i < n; i++){
        cin >> f[i];
    }

    sort(f, f+n);
}