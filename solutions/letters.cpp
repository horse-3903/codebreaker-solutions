#include <bits/stdc++.h>
using namespace std;
typedef unsigned long long ull;

int main(){
    char str[1000000];
    cin >> str;

    // state : dp[i] = no. of possible subsequences in order from str[0] to str[i]
    ull dp[1000000];

    // base case : dp[0] = 1
    dp[0] = 1;

    // transition : 
    for (ull i = 1; i < 1000000; i++){

    }
}